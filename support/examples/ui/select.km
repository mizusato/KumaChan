import assets from './assets';

do ui-init 'WebQuick Select' DOM;

do ui-inject-css [assets::style-css];

do UI::ExitWhenWindowClosed;

private const DOM: NoExcept*[UI::Node]
    ~ with-reactive* &(selection) := 'foo',
    UI::Styled ['container']
    (UI::Container [
        (UI::Styled ['row']
        (UI::Container [
            UI::Select selection [('foo','Foo'),('bar','Bar'),('42','42')],
            UI::TextWatch (selection.watch)
        ])),
        UI::Button 'reset' (selection.update (&(_)('foo')))
    ]);
