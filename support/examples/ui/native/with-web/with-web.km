do Window.{show-at-center};

do Window.{on-close}.{connect {exit 0}};

do
    & buf := reactive '',
    { merge [
        (InputBox bind buf),
        (MainView bind { ui-container [
            { ui-label 'Echo:' },
            { ui-label-dynamic buf.{watch} }
        ] })
    ] };
