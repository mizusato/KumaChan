do scanln
    . { then &(line) =>
    	let result := { json-parse line },
    	switch result:
    	case OK v:
    	    { println { json-stringify v } },
    	case NG err:
    	    { errorln err },
    	end
    }
    . { crash-on-error };
