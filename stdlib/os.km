module OS;

export const Stdin  := (I/O::InputStream)  native 'Stdin';
export const Stdout := (I/O::OutputStream) native 'Stdout';
export const Stderr := (I/O::OutputStream) native 'Stderr';

global exit := (&Int.Effect([],[]))  native 'exit';
