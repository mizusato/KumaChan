public function trace[T] {
    & (T) Debug[T]:
    native 'trace'
    /// side-effect: log input value in stdout
};
public function panic[T] {
    & (String) Never[T]:
    native 'panic'
    /// side-effect: crash immediately
};
