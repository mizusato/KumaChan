## Debugging Functions

/// trace(v) writes the description of v into stderr and returns it back.
export function trace:[T]
    &(T) => T
    native 'trace';

/// panic(msg) causes a panic with message msg.
export function panic:
    &(String) => never
    native 'panic';
