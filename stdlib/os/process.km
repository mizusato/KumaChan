export const Env: Map[String,String] :=
    native 'OS::Env';
export const Args: Array[String] :=
    native 'OS::Args';

type ProcessExit protected Action;
export function exit:
    &(Uint8) => ProcessExit
    native 'exit';
