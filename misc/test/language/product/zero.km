type Real Float;

type Point {
     x: Real,
     y: Real
};

export function zero-value:
    &() => Real
    &() => 0.0;

do
    let p := { Point {} },
    let {x,y} := p,
    { println { "(#,#)" (x.{str}, y.{str}) } }
        . { crash-on-error };
