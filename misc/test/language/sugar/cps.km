do
    | err := catch
        | await { println 'Number a:' },
        | str-a := await { scanln () },
        | await { println 'Number b:' },
        | str-b := await { scanln () },
        let sum? :=
            | a := get str-a.{parse-real},
            | b := get str-b.{parse-real},
            { Some (a + b) },
        switch sum?:
        case Some sum:
            { println { str sum }.{trace} },
        case None:
            { println 'None' },
        end,
    { crash err };
