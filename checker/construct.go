package checker
/*
import "kumachan/loader"


func ConstructTo(g *GenericType, expr Expr, ctx TypeContext) (Expr, *ExprError) {
	var reg = ctx.Ireg.(TypeRegistry)
	switch gv := g.Value.(type) {
	case Wrapped:
		var inferred = make(map[uint]Type, g.Arity)
		InferArgs(gv.InnerType, expr.Type, inferred)
	default:

	}
}*/