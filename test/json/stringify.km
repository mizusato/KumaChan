do
    let v :=
        (JSON::Array [
            (JSON::Number 1.23e30),
            (JSON::String '12aaa4'),
            (JSON::Boolean Yes),
            (JSON::Object (new-map [
                ('ABC', (JSON::Number 1.0)),
                ('DEF', (JSON::Boolean No)),
            	('foobar', (JSON::Array [
                    (JSON::Null),
                    (JSON::String 'baz')
            	]))
            ]))
        ]),
    println (json-stringify v)
            | catch (&(_)(crash 'I/O Error'));
