do (
    ~ for &(x) := emit* range(0,10):
    ~ do println (str x):
         println '---'
) | catch* &(_)(crash 'I/O Error')
  | wait-complete;

/*
do
	yield-range(0,10)
	| concat-map &(x)(
	    println str x
	    | then println '---'
	)
	| catch* &(_)(crash 'I/O Error')
	| wait-complete;
*/
