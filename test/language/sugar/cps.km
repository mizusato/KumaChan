do
    & err := catch
        & await { println 'Number a:' },
        & str-a := await { scanln () },
        & await { println 'Number b:' },
        & str-b := await { scanln () },
        let sum? :=
            & a := maybe? str-a.{parse-float},
            & b := maybe? str-b.{parse-float},
            { Just (a + b) },
        switch sum?:
        case Just sum:
            { println { str sum } },
        case N/A:
            { println 'N/A' },
        end,
    { crash err };
